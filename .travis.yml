language: python
python:
  - "2.7"
env:
  global:
     - DJANGO_TEST_TEMP_DIR=$TRAVIS_BUILD_DIR/slack/settings
  matrix:
     - DJANGO_VERSION=1.8.4
# command to install dependencies
install:
  - "pip install -r requirements.txt"
# command to run tests
script:
  - "cd thirtysevenjobs && python manage.py test"
